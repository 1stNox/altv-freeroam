using System;
using AltV.Net.Elements.Entities;

namespace AltV.Freeroam.Structure
{
    public class ServerColShape : ColShape
    {
        public ServerColShape(IntPtr nativePointer) : base(nativePointer)
        {
            
        }
    }
}